[project]
name = "ZoSolvers"
version = "0.1.5"
description = "Zeroth-order optimisation solvers with Gaussian and sphere random oracles"
authors = [
    {name = "amirali",email = "amirali77farzin@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy (>=2.2.6,<3.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)"
]

[tool.poetry]
packages = [{include = "ZoSolvers", from = "src"}]


[tool.poetry.group.dev.dependencies]
pytest = "^9.1.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
